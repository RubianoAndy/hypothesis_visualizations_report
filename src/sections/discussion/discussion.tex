\section{El papel de la visualización en el contraste de hipótesis}
    \label{seccion_visualizacion}
    \subsection{Lo que la figura interactiva revela}
        La Figura \ref{fig_plotly} presenta esa vista, construida con Plotly, 
        donde aparecen tres rectas de pendiente positiva, aproximadamente 
        paralelas y separadas verticalmente por el nivel de cada sector.

        \begin{strip}
            \centering
            \includegraphics[width=0.90\linewidth]{assets/images/figures/python/hypothesis/dispersion_sectores.png}
            \captionof{figure}{Consumo frente a temperatura desagregado por sector, con una
            recta de regresión ajustada dentro de cada grupo. Versión estática de la
            figura interactiva construida con Plotly, que en el navegador permite
            aislar sectores desde la leyenda, hacer zoom y consultar el
            identificador y la región de cada cliente}
            \label{fig_plotly}
        \end{strip}

        Aislar un sector desde la leyenda permite visualizar el análisis
        condicionado de la Tabla \ref{correlacion}, mientras que el
        \textit{hover} permite distinguir si un punto extremo corresponde a una
        región cálida o a un cliente atípico, algo que un gráfico estático no
        permite.

    \subsection{¿Por qué la correlación cae y la pendiente no?}
        El coeficiente de Pearson y la pendiente de la regresión se relacionan 
        mediante la siguiente ecuación:

        \begin{equation}
            r = b_1 \, \frac{s_T}{s_Y}
            \label{relacion_r_b}
        \end{equation}

        La temperatura se asigna independientemente del sector, como
        confirma la igualdad de medias ($F=0{,}23$; $p=0{,}798$). Por tanto, la
        pendiente global ($b_1=3{,}42$) se mantiene cercana al valor 4 de la
        ecuación (\ref{generador}), mientras que la separación entre sectores
        aumenta la variabilidad del consumo y reduce la correlación global
        (Tabla \ref{atenuacion}).

        \begin{table}[H]
            \centering
            \caption{Efecto de la agregación sobre la correlación}
            \begin{center}
                \begin{tabular}{|l|c|c|c|}
                    \hline
                    \textbf{Ámbito} & \textbf{$b_1$} & \textbf{$s_Y$} &
                    \textbf{$r$} \\
                    \hline
                    Residencial & 4,17 & 40,57  & 0,595 \\
                    \hline
                    Comercial   & 2,90 & 95,54  & 0,183 \\
                    \hline
                    Industrial  & 7,17 & 195,26 & 0,212 \\
                    \hline
                    \textbf{Global} & \textbf{3,42} & \textbf{317,49} &
                    \textbf{0,063} \\
                    \hline
                \end{tabular}
                \label{atenuacion}
            \end{center}
        \end{table}

        El efecto estimado se mantiene tras la agregación, pero la
        asociación estandarizada se atenúa. Con $s_T=5{,}84$, la ecuación
        (\ref{relacion_r_b}) produce $r=0{,}063$, igual al valor observado. No
        hay inversión del signo, sino una atenuación por dilución: la
        variabilidad entre sectores actúa como ruido frente a una señal que
        permanece.

        \begin{strip}
            \centering
            \includegraphics[width=0.90\linewidth]{assets/images/figures/python/hypothesis/atenuacion.png}
            \captionof{figure}{Descomposición gráfica de la identidad $r = b_1 s_T / s_Y$
            sobre los cuatro ámbitos de análisis (seaborn). El panel izquierdo
            muestra que la pendiente global se mantiene cerca del valor de diseño;
            el central, que la desviación del consumo se multiplica por casi ocho
            al mezclar los sectores; el derecho, la consecuencia sobre la
            correlación}
            \label{fig_atenuacion}
        \end{strip}

        La Figura \ref{fig_atenuacion} traduce esa aritmética en tres paneles que
        pueden leerse de izquierda a derecha como una cadena causal. La pendiente
        global, 3,42, se sitúa dentro del rango que definen las tres pendientes
        sectoriales y próxima al valor de diseño; la desviación del consumo salta
        de 40,6 kWh en Residencial a 317,5 en el agregado, un factor cercano a
        ocho; y la correlación se desploma en la misma proporción en que crece ese
        denominador. Presentar los tres términos por separado evita el error de
        interpretación más frecuente ante un $r$ pequeño, que es atribuirlo a la
        ausencia de efecto en lugar de a un cambio de escala en la variable
        dependiente.

        \vspace{10pt}

        Conviene subrayar la asimetría práctica de este resultado. Si el objetivo
        fuera estimar cuánto aumenta el consumo por cada grado adicional, el modelo
        agregado daría una respuesta aceptable. Si el objetivo es decidir si vale la
        pena incorporar la temperatura como variable de planeación, el mismo modelo
        conduciría a descartarla. La prueba de hipótesis responde a la segunda
        pregunta y, sin la figura, lo haría de forma equivocada.

    \subsection{Síntesis en un tablero interactivo}
        \label{subsec_dashboard}
        Las figuras anteriores atienden cada una a una hipótesis. El paso final es
        reunirlas en una sola vista que permita confrontarlas entre sí, porque la
        contradicción que articula este informe solo aparece al poner un resultado
        junto a otro. La Figura \ref{fig_dashboard} presenta ese tablero.

        \begin{strip}
            \centering
            \includegraphics[width=0.90\linewidth]{assets/images/figures/python/hypothesis/dashboard.png}
            \captionof{figure}{Tablero interactivo que reúne las cuatro decisiones del
            protocolo (Plotly). El código de color del panel B distingue las
            hipótesis rechazadas de las que no lo son. Versión estática de la
            figura interactiva, que en el navegador permite hacer zoom, aislar
            series y consultar el identificador y la región de cada cliente}
            \label{fig_dashboard}
        \end{strip}

        El panel A establece que el sector separa las distribuciones sin traslape, 
        el panel B muestra que ese mismo conjunto de datos declara no significativa 
        la correlación global mientras la del sector Residencial es la mayor de las 
        cuatro, el panel C exhibe la recta agregada atravesando tres franjas que no 
        describe y el panel D identifica el término responsable, la desviación del 
        consumo, que en el ámbito global supera incluso a la del sector más disperso. 
        Los cuatro paneles proceden de los mismos 300 registros, y es su yuxtaposición 
        la que convierte un conjunto de valores $p$ en un diagnóstico.

    \subsection{Verificación cruzada y reproducibilidad}
        La Tabla \ref{verificacion} compara las cifras obtenidas en ambos entornos,
        la coincidencia dígito a dígito confirma que los resultados son propiedad de
        los datos y no del software empleado.

        \begin{table}[H]
            \centering
            \caption{Verificación cruzada entre Python y R}
            \begin{center}
                \begin{tabular}{|l|l|l|}
                    \hline
                    \textbf{Prueba} & \textbf{Python} & \textbf{R} \\
                    \hline
                    Shapiro-Wilk (Res.) & 0,9912 & 0,9912 \\
                    \hline
                    t de Welch & $-23{,}5384$ & $-23{,}5384$ \\
                    \hline
                    ANOVA & $F = 775{,}9887$ & $F = 775{,}9887$ \\
                    \hline
                    Tukey (Res.$-$Ind.) & $-700{,}698$ & $-700{,}698$ \\
                    \hline
                    Pearson global & $r = 0{,}063$ & $r = 0{,}063$ \\
                    \hline
                    $d$ de Cohen & 3,3288 & 3,3288 \\
                    \hline
                    $\eta^2$ / $\omega^2$ & 0,8394 / 0,8378 & 0,8394 / 0,8378 \\
                    \hline
                    ANOVA de Welch & $F = 830{,}2239$ & $F = 830{,}2239$ \\
                    \hline
                    Games-Howell (Ind.$-$Res.) & $+700{,}698$ & $+700{,}698$ \\
                    \hline
                    Varianzas & Levene $= 60{,}43$ & Bartlett $= 199{,}39$ \\
                    \hline
                \end{tabular}
                \label{verificacion}
            \end{center}
        \end{table}

        Los tamaños de efecto, la potencia y el post-hoc de Games-Howell se programaron 
        de cero en ambos entornos a partir de sus fórmulas cerradas, sin recurrir a 
        paquetes especializados y aun así los cuatro primeros decimales coinciden, siendo 
        una verificación más exigente que la de las pruebas estándar, donde dos 
        implementaciones maduras del mismo algoritmo podrían coincidir por compartir 
        su origen.

        \vspace{10pt}

        La única discrepancia corresponde a la homogeneidad de varianzas y es
        esperada, puesto que Levene se apoya en las desviaciones absolutas 
        respecto de la media y Bartlett en el cociente de verosimilitudes bajo 
        el supuesto de normalidad que en este caso conducen a la misma decisión.

        \vspace{10pt}

        Las nueve figuras se replicaron íntegramente con ggplot2 sobre los estadísticos 
        recalculados en R. A continuación, cuatro de ellas demuestran que ambas cadenas 
        de herramientas conducen a la misma lectura.

        \vspace{10pt}

        La primera réplica en R verifica el supuesto de normalidad mediante un
        histograma equivalente al de Python. Ambas herramientas producen la misma
        conclusión visual: la distribución se alinea con la curva normal teórica.

        \begin{figure}[H]
            \centering
            \includegraphics[width=0.95\linewidth]{assets/images/figures/r/hypothesis/histograma_normalidad.png}
            \caption{Réplica en ggplot2: verificación de normalidad del sector
            Residencial}
            \label{fig_r_normalidad}
        \end{figure}

        La segunda réplica presenta las medias con intervalos de confianza del 95\,\%
        y las letras de Tukey que codifican las diferencias significativas. La
        Figura \ref{fig_r_medias} es idéntica en estructura a la Figura
        \ref{fig_medias}, confirmando que los tres grupos difieren entre sí sin
        ambigüedad.

        \begin{figure}[H]
            \centering
            \includegraphics[width=0.95\linewidth]{assets/images/figures/r/hypothesis/medias_ic95.png}
            \caption{Réplica en ggplot2: consumo medio por sector con intervalos de
            confianza del 95\,\% y letras de Tukey}
            \label{fig_r_medias}
        \end{figure}

        La tercera réplica muestra las comparaciones múltiples con ambos post-hoc:
        Tukey (punteado) y Games-Howell (sólido). Este forest plot es la traducción
        gráfica de la Tabla \ref{robustez}, permitiendo ver de un vistazo que ambos
        métodos conducen a la misma decisión: ningún intervalo cruza el cero.

        \begin{figure}[H]
            \centering
            \includegraphics[width=0.95\linewidth]{assets/images/figures/r/hypothesis/tukey_forest.png}
            \caption{Réplica en ggplot2: comparaciones múltiples de Tukey y
            Games-Howell}
            \label{fig_r_forest}
        \end{figure}

        La coincidencia dígito a dígito entre estas tres réplicas en R y sus
        homólogas en Python (Figuras \ref{fig_normalidad}, \ref{fig_medias} y
        \ref{fig_forest}) demuestra que ni las cifras ni los gráficos dependen de
        un único entorno. Los mismos datos procesados en dos lenguajes distintos
        producen exactamente el mismo resultado, lo que constituye el nivel más
        alto de garantía sobre la exactitud de los cálculos.

        \clearpage

        \begin{strip}
            \centering
            \includegraphics[width=0.90\linewidth]{assets/images/figures/r/hypothesis/dashboard.png}
            \captionof{figure}{Réplica en ggplot2 del tablero de la Figura
            \ref{fig_dashboard}, compuesta en una retícula de cuatro paneles y
            exportada también como versión interactiva mediante \texttt{ggplotly}}
            \label{fig_ggplot_extra}
        \end{strip}

    \subsection{Limitaciones}
        Tres advertencias delimitan el alcance de los resultados. La primera es que
        los datos son simulados y su estructura sectorial se conoce de antemano; en
        un conjunto real la variable que induce la agregación podría no estar
        registrada, lo que refuerza el argumento central de este informe, ya que la
        figura sería entonces el único indicio disponible de que el modelo agregado
        es inadecuado. La segunda es que el análisis se limita a la relación lineal
        entre dos variables: la correlación de Pearson y el modelo de la ecuación
        (\ref{ols}) no capturarían una dependencia en forma de U, que un modelo
        multivariante con el sector como covariable o un ajuste no paramétrico sí
        detectarían. La tercera es que el efecto de la temperatura se introdujo
        idéntico en los tres sectores, de modo que el escenario examinado es el de
        rectas paralelas; si las pendientes difirieran entre grupos habría que
        contrastar además el término de interacción, y la conclusión sobre qué
        segmento incorporar a la planeación podría cambiar.

        \vspace{10pt}

        Dos reservas que habrían figurado aquí quedan resueltas en el cuerpo del
        informe. El incumplimiento de la homocedasticidad no se deja como
        advertencia sino que se contrasta en la Sección
        \ref{subsec_robustez_resultados}, donde el ANOVA de Welch y el post-hoc de
        Games-Howell reproducen la decisión del procedimiento clásico
        \cite{montgomery2020design}. Y la sospecha de que la significancia de las
        comparaciones múltiples se deba al tamaño muestral se descarta en la
        Sección \ref{subsec_efecto_resultados}: con $d = 3{,}33$ y
        $\omega^2 = 0{,}838$, las diferencias serían significativas con una
        fracción de las observaciones disponibles.